configVars = {
    
    // File name options: ic_{prefix}_{slice name}_{suffix}_{size}[@2x].png 
    // Examples: ic_btn_send_pressed_24.png, ic_action_delete.png, ic_star@2x.png
    
    // Include a prefix? [yes/no]
    includePrefix: "yes", 
    
	// By default, you'll be prompted to enter a prefix (examples: launcher, menu, stat_notify, tab, dialog)
	// Un-comment the next line to set a custom prefix instead: 
	// prefixName: "prefix",
	
    
    // Do you want to include a suffix? [yes/no]
    includeSuffix: "yes",
    
    // The default suffix is set to the page name (examples: normal, pressed, focused, disabled, selected)
    // Un-comment the next line to set a custom suffix instead:
	// suffixName: "suffix",
	
	
	// Include the asset width at the end of the file name? [yes/no]
	size: "no"
	
}
